export default function NotFoundPage() {
  document.title = 'Not Found - CIMSA ULM';

  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      }}
    >
      <h1>Not Found</h1>
    </div>
  );
}
